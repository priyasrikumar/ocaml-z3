(ocamllex Smtlib_lexer)

(menhir
 (modules Smtlib_parser))

(library
  (name smt)
  (libraries bignum z3)
  (virtual_deps menhir)
  (inline_tests)
  (ocamlc_flags -custom -cclib -lstdc++)
  (ocamlopt_flags -cclib -lstdc++))